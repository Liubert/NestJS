---
phase: quick-260405-q5q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/translations/sandbox.controller.ts
  - mcp-server/src/tools/production.ts
  - mcp-server/src/permissions.ts
  - mcp-server/src/tools/environment.ts
  - mcp-server/src/prompts.ts
  - mcp-server/README.md
  - mcp-server/AGENT_GUIDE.md
  - mcp-server/PROJECT_OVERVIEW.md
  - mcp-server/flows/onboarding-flow.yaml
autonomous: true
requirements: [remove-init-sandbox]
must_haves:
  truths:
    - "POST /sandbox/init endpoint no longer exists"
    - "init_sandbox MCP tool no longer registered"
    - "All MCP docs reference auto-init or reset_sandbox instead of init_sandbox"
    - "resetSandbox and auto-init logic remain untouched"
  artifacts:
    - path: "src/modules/translations/sandbox.controller.ts"
      provides: "Sandbox controller without init endpoint"
      contains: "reset("
    - path: "mcp-server/src/tools/production.ts"
      provides: "Production tools without init_sandbox"
      contains: "reset_sandbox"
    - path: "mcp-server/src/permissions.ts"
      provides: "Tool registry without init_sandbox"
  key_links:
    - from: "mcp-server/src/tools/production.ts"
      to: "sandbox.controller.ts"
      via: "reset_sandbox still calls /sandbox/reset"
      pattern: "sandbox/reset"
---

<objective>
Remove the dead `init_sandbox` tool and `POST /sandbox/init` endpoint from the entire codebase. Sandbox is auto-initialized on project creation, and `reset_sandbox` covers the force-reset case.

Purpose: Dead code cleanup — `init_sandbox` is never needed by users/agents since auto-init exists.
Output: Clean codebase with no references to init_sandbox; docs updated to reflect auto-init reality.
</objective>

<execution_context>
@/Users/liubomyrfedyshyn/WebstormProjects/nest_js/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liubomyrfedyshyn/WebstormProjects/nest_js/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/modules/translations/sandbox.controller.ts
@mcp-server/src/tools/production.ts
@mcp-server/src/permissions.ts
@mcp-server/src/prompts.ts
@mcp-server/src/tools/environment.ts
@mcp-server/README.md
@mcp-server/AGENT_GUIDE.md
@mcp-server/PROJECT_OVERVIEW.md
@mcp-server/flows/onboarding-flow.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove init_sandbox endpoint and MCP tool registration</name>
  <files>
    src/modules/translations/sandbox.controller.ts,
    mcp-server/src/tools/production.ts,
    mcp-server/src/permissions.ts
  </files>
  <action>
1. In `src/modules/translations/sandbox.controller.ts`:
   - Delete the `InitSandboxDto` class (lines 33-37) — it is defined inline in this file, not imported from a DTO file
   - Delete the `@Post('init')` endpoint method `init()` (lines 60-76)
   - Remove `IsBoolean` and `IsOptional` from the class-validator import if no longer used (check remaining code — they are NOT used elsewhere in this file)

2. In `mcp-server/src/tools/production.ts`:
   - Delete the first `server.tool('init_sandbox', ...)` block (lines 27-61)
   - Remove the `logWrite` import if it is no longer used after removing init_sandbox (check: `logWrite` is NOT used in `reset_sandbox` or other tools in this file — verify by searching the file)

3. In `mcp-server/src/permissions.ts`:
   - Delete the `init_sandbox` line from `TOOL_REGISTRY` (line 55)
  </action>
  <verify>
    <automated>cd /Users/liubomyrfedyshyn/WebstormProjects/nest_js && grep -r "init_sandbox" src/ --include="*.ts" | grep -v "node_modules" | grep -v ".spec." ; echo "EXIT:$?"</automated>
    Grep for `init_sandbox` in `src/` and `mcp-server/src/tools/production.ts` and `mcp-server/src/permissions.ts` should return zero matches. Also: `npm run lint:js` passes for modified files.
  </verify>
  <done>POST /sandbox/init endpoint removed, init_sandbox MCP tool unregistered, tool registry cleaned. No TypeScript compile errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update all MCP docs and prompts to remove init_sandbox references</name>
  <files>
    mcp-server/src/prompts.ts,
    mcp-server/src/tools/environment.ts,
    mcp-server/README.md,
    mcp-server/AGENT_GUIDE.md,
    mcp-server/PROJECT_OVERVIEW.md,
    mcp-server/flows/onboarding-flow.yaml
  </files>
  <action>
1. In `mcp-server/src/prompts.ts`:
   - Line 19: Replace `"- \`NOT initialized\` → call \`init_sandbox\` first"` with `"- \`NOT initialized\` → sandbox auto-initializes on project creation; use \`reset_sandbox\` if re-sync needed"`
   - Line 99: Replace `"→ After fixing: call init_sandbox if sandbox is not initialized"` with `"→ After fixing: sandbox auto-initializes; use \`reset_sandbox\` to re-sync if needed"`
   - Line 112: Remove `init_sandbox` from the create flow line — change to `"→ If create now: ask for slug confirmation, then call create_project, create_namespace, create_locale"` (sandbox auto-inits on project creation)
   - Line 140: Replace `"- init_sandbox must be called before any sandbox writes"` with `"- Sandbox is auto-initialized on project creation; no manual init needed"`
   - Line 246: Replace the NOT initialized message — change `"❌ NO — call init_sandbox before writing"` to `"❌ NO — use reset_sandbox to re-sync from production"`

2. In `mcp-server/src/tools/environment.ts`:
   - Line 275: Replace `"| \`NOT initialized\` | Call \`init_sandbox({ projectSlug })\` before any write |"` with `"| \`NOT initialized\` | Sandbox auto-initializes on project creation. Use \`reset_sandbox\` to re-sync if needed |"`

3. In `mcp-server/README.md`:
   - Delete the `init_sandbox` row from the Sandbox & Promotion tools table (line 188)
   - Update tool count: line 113 change `Available Tools (39 total)` to `Available Tools (38 total)`
   - Lines 208 and 240: Remove the `init_sandbox` step from workflow examples. Replace with a comment that sandbox is auto-initialized on project creation, or simply remove the line and adjust step numbering.

4. In `mcp-server/AGENT_GUIDE.md`:
   - Line 57: Replace `"| \`NOT initialized\` | Call \`init_sandbox({ projectSlug })\` before any write |"` with `"| \`NOT initialized\` | Sandbox auto-initializes on project creation. Use \`reset_sandbox({ projectSlug, confirmed: true })\` to re-sync |"`
   - Line 237: Replace `"call create_namespace, create_locale, and init_sandbox"` with `"call create_namespace and create_locale (sandbox auto-initializes)"`
   - Lines 280+: Delete the entire `### \`init_sandbox\`` section (the tool reference block)
   - Line 354: Remove `"call init_sandbox if you plan to use the sandbox workflow"` — replace with note that sandbox auto-initializes
   - Line 649: Remove `init_sandbox({ projectSlug: "travis" })` from workflow example
   - Line 713: Replace `"→ call init_sandbox"` with `"→ use reset_sandbox to re-sync if needed"`
   - Line 823: Remove `init_sandbox` row from the tool permissions table

5. In `mcp-server/PROJECT_OVERVIEW.md`:
   - Line 121: Replace the `init_sandbox` mention with `reset_sandbox` — update to: `"Use the sandbox workflow — make changes there, review with \`get_translation_diff\`, and promote via Admin UI."`

6. In `mcp-server/flows/onboarding-flow.yaml`:
   - Line 109: Remove `init_sandbox` reference — sandbox auto-initializes
   - Line 128: Remove `Call: init_sandbox` step
   - Line 263: Remove `init_sandbox` from the numbered steps (renumber remaining)
   - Line 363: Remove `init_sandbox` reference

Do NOT touch any references to `reset_sandbox`, `resetSandbox`, or auto-init logic.
  </action>
  <verify>
    <automated>cd /Users/liubomyrfedyshyn/WebstormProjects/nest_js && grep -r "init_sandbox" mcp-server/ --include="*.ts" --include="*.md" --include="*.yaml" | wc -l | tr -d ' '</automated>
    Should return 0. No remaining references to `init_sandbox` in the mcp-server directory.
  </verify>
  <done>All documentation, prompts, and flow files updated. Zero references to init_sandbox remain in mcp-server/. reset_sandbox and auto-init guidance is in place everywhere init_sandbox was previously mentioned.</done>
</task>

</tasks>

<verification>
1. `grep -r "init_sandbox" src/ mcp-server/ --include="*.ts" --include="*.md" --include="*.yaml"` returns zero results
2. `npm run lint:js` passes
3. `cd mcp-server && npx tsc --noEmit` compiles without errors
4. `POST /sandbox/init` no longer appears in Swagger when server starts
5. `reset_sandbox` tool and `POST /sandbox/reset` endpoint remain functional
</verification>

<success_criteria>
- Zero references to `init_sandbox` in the entire codebase (src/ and mcp-server/)
- InitSandboxDto class removed
- Tool count in README updated to 38
- All former `init_sandbox` guidance replaced with auto-init explanation or reset_sandbox reference
- No changes to resetSandbox service method, reset_sandbox MCP tool, or auto-init logic
</success_criteria>

<output>
After completion, create `.planning/quick/260405-q5q-remove-init-sandbox-dead-tool-replaced-b/260405-q5q-SUMMARY.md`
</output>
