---
phase: quick
plan: 260405-ng9
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp-server/src/tools/ai.ts
  - mcp-server/src/tools/sandbox-writes.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "ai_translate output tells the agent to use bulk_import for multi-locale saves and bulk_set_locale for single-locale saves"
    - "Sandbox warning only fires when both hasChanges is true AND snapshotCount > 0"
  artifacts:
    - path: "mcp-server/src/tools/ai.ts"
      provides: "Improved save guidance after ai_translate"
      contains: "bulk_import"
    - path: "mcp-server/src/tools/sandbox-writes.ts"
      provides: "Reduced sandbox warning noise"
      contains: "snapshotCount"
  key_links: []
---

<objective>
Fix two MCP tool guidance issues: (1) ai_translate gives vague save instructions causing agent trial-and-error, (2) sandbox warning fires misleadingly when there are no prior snapshots.

Purpose: Reduce agent confusion and unnecessary warning noise during translation workflows.
Output: Two updated MCP tool files with precise guidance and smarter warning logic.
</objective>

<execution_context>
@/Users/liubomyrfedyshyn/WebstormProjects/nest_js/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liubomyrfedyshyn/WebstormProjects/nest_js/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@mcp-server/src/tools/ai.ts
@mcp-server/src/tools/sandbox-writes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ai_translate save guidance and sandbox warning condition</name>
  <files>mcp-server/src/tools/ai.ts, mcp-server/src/tools/sandbox-writes.ts</files>
  <action>
**File 1: mcp-server/src/tools/ai.ts**

On line 88, replace the current guidance string:
```
`Use set_translation or bulk_import to save these translations.`
```
With:
```
`To save: use bulk_import for all locales at once ({ "locale": { "key": "value" } } format), or bulk_set_locale for a single locale only.`
```

This tells the agent exactly which tool to pick based on whether it has translations for multiple locales (bulk_import) or just one (bulk_set_locale), eliminating trial-and-error.

**File 2: mcp-server/src/tools/sandbox-writes.ts**

In the `getSandboxWarning` function (line 41), change the condition from:
```typescript
if (status.hasChanges) {
```
To:
```typescript
if (status.hasChanges && status.snapshotCount > 0) {
```

Rationale: when snapshotCount === 0, there are no saved snapshots from a previous session to review. The warning about "pending changes" is misleading noise when the agent is actively writing in its current session for the first time. Only warn when there are actual prior snapshots the agent should review before adding more changes.
  </action>
  <verify>
    <automated>cd /Users/liubomyrfedyshyn/WebstormProjects/nest_js && grep -n "bulk_import" mcp-server/src/tools/ai.ts | grep -q "bulk_set_locale" && grep -n "snapshotCount > 0" mcp-server/src/tools/sandbox-writes.ts | grep -q "hasChanges" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - ai_translate output references bulk_import and bulk_set_locale with clear usage guidance
    - getSandboxWarning only triggers when hasChanges AND snapshotCount > 0
    - No other logic changes
  </done>
</task>

<task type="auto">
  <name>Task 2: Build MCP server to verify no compile errors</name>
  <files>mcp-server/src/tools/ai.ts, mcp-server/src/tools/sandbox-writes.ts</files>
  <action>
Run the MCP server TypeScript build to confirm the changes compile cleanly. If the project has a build or typecheck script, use it. Otherwise run `npx tsc --noEmit` from the mcp-server directory.
  </action>
  <verify>
    <automated>cd /Users/liubomyrfedyshyn/WebstormProjects/nest_js/mcp-server && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>TypeScript compilation succeeds with zero errors</done>
</task>

</tasks>

<verification>
- grep confirms "bulk_import" and "bulk_set_locale" in ai.ts guidance line
- grep confirms "snapshotCount > 0" in sandbox-writes.ts condition
- TypeScript compiles without errors
</verification>

<success_criteria>
1. ai_translate tool output provides actionable save guidance naming bulk_import and bulk_set_locale with format hints
2. Sandbox warning only appears when there are both pending changes AND prior snapshots (snapshotCount > 0)
3. MCP server builds cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260405-ng9-fix-ai-translate-save-guidance-and-sandb/260405-ng9-SUMMARY.md`
</output>
