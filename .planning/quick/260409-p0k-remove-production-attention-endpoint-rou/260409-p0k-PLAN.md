---
phase: quick
plan: 260409-p0k
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/translations/translations.controller.ts
  - src/modules/translations/translations.service.ts
  - .planning/ENDPOINT-INVENTORY.md
autonomous: true
must_haves:
  truths:
    - "Production attention endpoint no longer exists (GET /projects/:slug/namespaces/:ns/attention returns 404)"
    - "Sandbox attention endpoint still works (GET /sandbox/namespaces/:ns/attention returns 200)"
    - "MCP tool get_translations_needing_attention still calls sandbox endpoint and works unchanged"
    - "Application compiles without errors after dead code removal"
  artifacts:
    - path: "src/modules/translations/translations.controller.ts"
      provides: "Controller without production attention endpoint"
      contains_not: "getAttentionItems"
    - path: "src/modules/translations/translations.service.ts"
      provides: "Service without production getAttentionItems method"
      contains_not: "async getAttentionItems"
  key_links:
    - from: "mcp-server/src/tools/translations.ts"
      to: "sandbox.controller.ts GET /sandbox/namespaces/:ns/attention"
      via: "apiGet call to sandbox path"
      pattern: "/sandbox/namespaces/.*/attention"
---

<objective>
Remove the production attention endpoint and its service method. The production endpoint reads from `translation_values` (production table) which is incorrect -- attention items should always come from sandbox where active editing happens. The sandbox endpoint already exists and the MCP tool already points to it.

Purpose: Eliminate a misleading/incorrect endpoint that could cause confusion if called directly, and remove ~150 lines of dead production-table code.
Output: Cleaner controller/service, updated endpoint inventory.
</objective>

<execution_context>
@/Users/liubomyrfedyshyn/WebstormProjects/nest_js/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liubomyrfedyshyn/WebstormProjects/nest_js/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/modules/translations/translations.controller.ts
@src/modules/translations/translations.service.ts
@.planning/ENDPOINT-INVENTORY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove production attention endpoint and service method</name>
  <files>src/modules/translations/translations.controller.ts, src/modules/translations/translations.service.ts</files>
  <action>
1. In `translations.controller.ts`, DELETE the entire `getAttentionItems` method (lines 746-769):
   - The `@Get('projects/:slug/namespaces/:ns/attention')` route decorator
   - The `@UseGuards(JwtAuthGuard)`, `@ApiBearerAuth()`, `@ApiOperation` decorators
   - The `async getAttentionItems(...)` method body
   - This is between the disabled `updateEntry` comment block and the disabled `checkEntryQuality` comment block.

2. In `translations.service.ts`, DELETE the entire `getAttentionItems` method (lines 825-977):
   - Starts with `async getAttentionItems(projectSlug: string, nsSlug: string, ...)`
   - Ends just before `async createEntry(` at line 979
   - This is approximately 153 lines of dead code that queries `translation_values` (production table)

3. After deletion, verify no remaining references to the deleted method exist in the service or controller (there should be none -- no other code calls `translationsService.getAttentionItems`).

Do NOT touch:
- `sandbox.controller.ts` (its attention endpoint is the correct one)
- `sandbox.service.ts` (its `getSandboxAttentionItems` is the correct implementation)
- `mcp-server/src/tools/translations.ts` (already calls sandbox path `/translations/projects/${projectSlug}/sandbox/namespaces/${namespace}/attention`)
  </action>
  <verify>
    <automated>cd /Users/liubomyrfedyshyn/WebstormProjects/nest_js && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Production attention endpoint removed. TypeScript compiles cleanly. No references to the deleted method remain in the codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Update ENDPOINT-INVENTORY.md</name>
  <files>.planning/ENDPOINT-INVENTORY.md</files>
  <action>
1. Update row 27 (line 45) in ENDPOINT-INVENTORY.md:
   - Change from: `| 27 | GET | /translations/projects/:slug/namespaces/:ns/attention | TranslationsController | MCP: get_translations_needing_attention | active |`
   - Change to: `| 27 | GET | /translations/projects/:slug/namespaces/:ns/attention | TranslationsController | None (was incorrectly reading production table) | removed |`

2. Find the sandbox entries section (around rows 39-48) and ADD a new row for the sandbox attention endpoint that was missing:
   - Add after the sandbox entries listing (e.g. after the sandbox mark-expected DELETE row):
   - `| NN | GET | /translations/projects/:slug/sandbox/namespaces/:ns/attention | SandboxController | MCP: get_translations_needing_attention | active |`
   - Use the next sequential row number in the table.

This corrects the stale inventory: the MCP tool was never calling the production endpoint (it has always called the sandbox path), and the sandbox attention endpoint was missing from the inventory entirely.
  </action>
  <verify>
    <automated>cd /Users/liubomyrfedyshyn/WebstormProjects/nest_js && grep -n "attention" .planning/ENDPOINT-INVENTORY.md</automated>
  </verify>
  <done>Inventory row 27 marked as removed. New row added for sandbox attention endpoint with correct MCP consumer reference.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- compiles without errors
2. `grep -rn "getAttentionItems" src/modules/translations/translations.controller.ts src/modules/translations/translations.service.ts` -- returns nothing
3. `grep "attention" .planning/ENDPOINT-INVENTORY.md` -- shows row 27 as removed and new sandbox row as active
4. `grep "sandbox.*attention" mcp-server/src/tools/translations.ts` -- confirms MCP tool still points to sandbox path (unchanged)
</verification>

<success_criteria>
- Production attention endpoint removed (404 on GET /projects/:slug/namespaces/:ns/attention)
- ~153 lines of dead service code removed
- TypeScript compiles cleanly
- MCP tool unchanged and still functional via sandbox endpoint
- ENDPOINT-INVENTORY.md accurately reflects current state
</success_criteria>

<output>
After completion, create `.planning/quick/260409-p0k-remove-production-attention-endpoint-rou/260409-p0k-SUMMARY.md`
</output>
