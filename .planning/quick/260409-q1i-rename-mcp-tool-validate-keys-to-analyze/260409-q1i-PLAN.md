---
phase: quick
plan: 260409-q1i
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp-server/src/tools/sandbox-writes.ts
  - mcp-server/src/tools/project-management.ts
  - mcp-server/src/prompts.ts
  - mcp-server/src/permissions.ts
  - mcp-server/README.md
  - mcp-server/package.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "MCP tool 'analyze_entries' is registered and callable with the new name"
    - "No MCP tool named 'validate_keys' exists anywhere in the codebase"
    - "The 'validate_keys' tool in project-management.ts is renamed to 'check_keys_exist' (it is a distinct tool that checks key existence, not a dead duplicate)"
    - "MCP package version is bumped by one patch level"
    - "Quick Start guide in prompts.ts references 'analyze_entries' not 'validate_keys'"
  artifacts:
    - path: "mcp-server/src/tools/sandbox-writes.ts"
      provides: "analyze_entries tool registration with updated description"
      contains: "analyze_entries"
    - path: "mcp-server/src/tools/project-management.ts"
      provides: "check_keys_exist tool (renamed from validate_keys)"
      contains: "check_keys_exist"
    - path: "mcp-server/src/permissions.ts"
      provides: "Permission entries for analyze_entries and check_keys_exist"
    - path: "mcp-server/src/prompts.ts"
      provides: "Quick Start guide referencing analyze_entries"
      contains: "analyze_entries"
    - path: "mcp-server/package.json"
      provides: "Bumped version 1.3.8"
      contains: "1.3.8"
  key_links:
    - from: "mcp-server/src/tools/sandbox-writes.ts"
      to: "mcp-server/src/permissions.ts"
      via: "tool name string must match permission key"
      pattern: "analyze_entries"
    - from: "mcp-server/src/tools/project-management.ts"
      to: "mcp-server/src/permissions.ts"
      via: "tool name string must match permission key"
      pattern: "check_keys_exist"
---

<objective>
Rename MCP tool `validate_keys` to `analyze_entries` in sandbox-writes.ts and update its description to accurately convey what the tool does. Rename the separate `validate_keys` tool in project-management.ts to `check_keys_exist` since it is a distinct tool (checks whether keys exist in a namespace) and not a dead duplicate. Update all references across prompts, permissions, README, and bump MCP package version.

Purpose: The name `validate_keys` is misleading -- the tool in sandbox-writes.ts does far more than validate key names. It performs full batch analysis (duplicate detection, conflict detection, source text overlap). The tool in project-management.ts is a genuinely different tool that checks key existence, so it also needs a distinct name.
Output: All MCP tool registrations, permissions, docs, and guide updated; no reference to `validate_keys` remains.
</objective>

<execution_context>
@/Users/liubomyrfedyshyn/WebstormProjects/nest_js/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liubomyrfedyshyn/WebstormProjects/nest_js/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@mcp-server/src/tools/sandbox-writes.ts (lines 600-700 — validate_keys tool registration for batch analysis)
@mcp-server/src/tools/project-management.ts (lines 850-918 — validate_keys tool that checks key existence)
@mcp-server/src/prompts.ts (line 31 — Quick Start guide reference)
@mcp-server/src/permissions.ts (line 68 — validate_keys permission entry)
@mcp-server/README.md (line 151 — tool table entry)
@mcp-server/package.json (version field)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename validate_keys to analyze_entries in sandbox-writes.ts and update description</name>
  <files>mcp-server/src/tools/sandbox-writes.ts</files>
  <action>
In `mcp-server/src/tools/sandbox-writes.ts` around line 600:

1. Change the section comment from `// --- validate_keys ---` to `// --- analyze_entries ---`

2. Change the tool name string from `'validate_keys'` to `'analyze_entries'` (line 627)

3. Replace the description array (lines 628-634) with this new description that accurately conveys what the tool does:

```
[
  'Read-only preflight analysis of a planned batch of translation keys.',
  'Checks: (1) duplicate keys within the submitted batch,',
  '(2) conflicts with existing keys in the namespace (same key, different value),',
  '(3) source text overlap — signals a potential reuse opportunity, but NOT a hard rule (the same word can have different translations in different contexts).',
  'Call BEFORE bulk_translate_and_save or set_translation when adding many new keys.',
  'Does NOT create or modify anything.',
].join(' ')
```

No other changes to the tool logic, parameters, or response formatting.
  </action>
  <verify>
    <automated>cd /Users/liubomyrfedyshyn/WebstormProjects/nest_js && grep -c "analyze_entries" mcp-server/src/tools/sandbox-writes.ts && grep -c "validate_keys" mcp-server/src/tools/sandbox-writes.ts</automated>
  </verify>
  <done>sandbox-writes.ts contains "analyze_entries" (at least 1 match), zero matches for "validate_keys"</done>
</task>

<task type="auto">
  <name>Task 2: Rename validate_keys to check_keys_exist in project-management.ts, update all references, bump version</name>
  <files>mcp-server/src/tools/project-management.ts, mcp-server/src/prompts.ts, mcp-server/src/permissions.ts, mcp-server/README.md, mcp-server/package.json</files>
  <action>
**A. `mcp-server/src/tools/project-management.ts` (lines 850-918):**
This is a DIFFERENT tool from the one in sandbox-writes.ts. It checks whether a list of keys exist in a namespace (found vs missing). Rename it:

1. Change section comment from `// --- validate_keys ---` to `// --- check_keys_exist ---`
2. Change tool name from `'validate_keys'` to `'check_keys_exist'` (line 852)
3. Update the description to:
```
[
  'Check whether a list of translation keys exist in a namespace.',
  'Returns: found keys, missing keys.',
  'Use this to verify that all t() calls in a component are backed by server entries.',
].join(' ')
```
(Description stays the same since it was already accurate for this tool.)

No changes to tool logic.

**B. `mcp-server/src/prompts.ts` (line 31):**
Replace the `validate_keys` reference with `analyze_entries` and update the description text:
```
"- `analyze_entries` — **preflight before bulk create**: read-only analysis of planned keys — checks for batch duplicates, conflicts with existing keys, and source-text overlap (signals reuse opportunity, not a hard rule). Call before bulk_translate_and_save or set_translation when adding many new keys.",
```
Also add a line for `check_keys_exist` in the appropriate section (after `analyze_entries` or in the read section):
```
"- `check_keys_exist` — verify that a list of t() keys exist on the server (found vs missing)",
```
Place the `check_keys_exist` entry in section "### 2. Read translations" since it is read-only key existence checking.

**C. `mcp-server/src/permissions.ts` (line 68):**
Replace:
```
validate_keys: { env: "both", access: "read" },
```
With two entries:
```
analyze_entries: { env: "both", access: "read" },
check_keys_exist: { env: "both", access: "read" },
```

**D. `mcp-server/README.md` (line 151):**
Replace the validate_keys row with two rows:
```
| `analyze_entries` | Preflight analysis of planned keys — duplicates, conflicts, reuse signals | `projectSlug`, `namespace`, `entries`, `sourceLocale?` |
| `check_keys_exist` | Check if a list of keys exist in a namespace | `projectSlug`, `namespace`, `keys`, `env?` |
```

**E. `mcp-server/package.json`:**
Bump version from `"1.3.7"` to `"1.3.8"`.
  </action>
  <verify>
    <automated>cd /Users/liubomyrfedyshyn/WebstormProjects/nest_js && grep -rc "validate_keys" mcp-server/src/ mcp-server/README.md && echo "---" && grep "analyze_entries" mcp-server/src/permissions.ts && grep "check_keys_exist" mcp-server/src/permissions.ts && grep '"version"' mcp-server/package.json</automated>
  </verify>
  <done>Zero matches for "validate_keys" across all mcp-server/src/ files and README.md. permissions.ts has both analyze_entries and check_keys_exist entries. package.json shows version 1.3.8.</done>
</task>

</tasks>

<verification>
After both tasks:

1. `grep -r "validate_keys" mcp-server/src/ mcp-server/README.md` returns zero matches
2. `grep "analyze_entries" mcp-server/src/tools/sandbox-writes.ts mcp-server/src/prompts.ts mcp-server/src/permissions.ts mcp-server/README.md` returns matches in all 4 files
3. `grep "check_keys_exist" mcp-server/src/tools/project-management.ts mcp-server/src/prompts.ts mcp-server/src/permissions.ts mcp-server/README.md` returns matches in all 4 files
4. `cd mcp-server && npx tsc --noEmit` compiles without errors
5. `grep '"version": "1.3.8"' mcp-server/package.json` returns a match
</verification>

<success_criteria>
- No reference to "validate_keys" remains in mcp-server/src/ or mcp-server/README.md
- `analyze_entries` tool registered in sandbox-writes.ts with updated description conveying read-only preflight analysis, three check types, and "not a hard rule" nuance for source text overlap
- `check_keys_exist` tool registered in project-management.ts (renamed from validate_keys, distinct tool)
- Both tools have permission entries in permissions.ts
- Quick Start guide in prompts.ts references both new tool names
- README.md tool table has both new entries with correct parameter lists
- MCP package version is 1.3.8
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/quick/260409-q1i-rename-mcp-tool-validate-keys-to-analyze/260409-q1i-SUMMARY.md`
</output>
