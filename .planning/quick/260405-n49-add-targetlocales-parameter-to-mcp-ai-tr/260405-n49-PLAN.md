---
phase: quick
plan: 260405-n49
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp-server/src/tools/ai.ts
autonomous: true
must_haves:
  truths:
    - "ai_translate tool accepts optional targetLocales string array parameter"
    - "When targetLocales provided, only those locales (validated against project) are sent to backend"
    - "When targetLocales omitted, all non-default project locales are used (existing behavior)"
    - "Unknown locale codes in caller-supplied list are silently filtered out"
  artifacts:
    - path: "mcp-server/src/tools/ai.ts"
      provides: "ai_translate tool with optional targetLocales parameter"
  key_links:
    - from: "mcp-server/src/tools/ai.ts"
      to: "/translations/ai-translate"
      via: "apiPost body.targetLocales"
      pattern: "targetLocales"
---

<objective>
Add optional `targetLocales` parameter to the MCP `ai_translate` tool so callers can restrict translation to specific locales instead of always translating to all project locales.

Purpose: Efficiency — when adding a single new locale, there is no need to re-translate all existing locales.
Output: Updated `mcp-server/src/tools/ai.ts` with the new parameter, validation logic, and updated description.
</objective>

<execution_context>
@/Users/liubomyrfedyshyn/WebstormProjects/nest_js/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liubomyrfedyshyn/WebstormProjects/nest_js/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@mcp-server/src/tools/ai.ts
@src/modules/translations/dto/ai-translate.dto.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add targetLocales parameter with validation to ai_translate tool</name>
  <files>mcp-server/src/tools/ai.ts</files>
  <action>
In `mcp-server/src/tools/ai.ts`, modify the `ai_translate` tool registration:

1. **Add parameter to schema** (after `context`):
   ```
   targetLocales: z.array(z.string()).optional().describe(
     'Optional list of locale codes to translate into (e.g. ["uk", "nb-NO"]). '
     + 'When omitted, translates to all non-default project locales. '
     + 'Use this for efficiency when filling only specific locales.'
   )
   ```

2. **Update tool description** — append to the existing description array:
   `'Optionally accepts targetLocales array to restrict translation to specific locales instead of all project locales.'`

3. **Update handler signature** — destructure `targetLocales` as `callerLocales` (to avoid shadowing the computed `targetLocales` variable):
   `async ({ projectSlug, text, context, targetLocales: callerLocales })`

4. **Add validation logic** after fetching project locales (after the existing `project.locales.filter(...).map(...)` block):
   - Keep the existing fetch of project locales — it always runs (needed for validation).
   - Rename the existing computed variable to `allProjectLocales`.
   - If `callerLocales` is provided and non-empty:
     - Create a Set from `allProjectLocales` for O(1) lookup.
     - Filter `callerLocales` to only include codes present in `allProjectLocales`.
     - Use the filtered list as `targetLocales` for the API call.
     - If filtering removed any codes, add a note line to the output (e.g. "Note: ignored unknown locales: xx, yy").
   - If `callerLocales` is not provided: use `allProjectLocales` as before.

5. **Output formatting** — after the existing translation result lines, if any locales were filtered out, append a note line before the "Use set_translation..." line.

No other files need changes. The backend DTO already supports `targetLocales?: string[]`.
  </action>
  <verify>
    <automated>cd /Users/liubomyrfedyshyn/WebstormProjects/nest_js && npx tsc --project mcp-server/tsconfig.json --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - ai_translate tool schema includes optional targetLocales parameter
    - When callerLocales provided, only valid (project-known) locale codes are sent to backend
    - When callerLocales omitted, behavior is identical to before (all non-default project locales)
    - Unknown caller-supplied locales are silently dropped with a note in output
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --project mcp-server/tsconfig.json --noEmit` passes
- Manual review: tool schema shows `targetLocales` as optional array of strings
- Logic: when targetLocales provided, only intersection with project locales is used
</verification>

<success_criteria>
- ai_translate MCP tool accepts optional targetLocales parameter
- Provided locales are validated against actual project locales
- Omitting the parameter preserves existing translate-all behavior
- No backend changes required (DTO already supports it)
</success_criteria>

<output>
After completion, create `.planning/quick/260405-n49-add-targetlocales-parameter-to-mcp-ai-tr/260405-n49-SUMMARY.md`
</output>
