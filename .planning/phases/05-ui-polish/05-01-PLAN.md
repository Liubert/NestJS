---
phase: 05-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - admin-ui/src/pages/translations/components/types.ts
  - admin-ui/src/pages/translations/components/QualityBadge.tsx
  - admin-ui/src/pages/translations/components/FilterBar.tsx
  - admin-ui/src/pages/translations/components/EntryEditModal.tsx
  - admin-ui/src/pages/translations/components/columns.tsx
  - admin-ui/src/pages/translations/components/api.ts
  - admin-ui/src/pages/translations/TranslationsPage.tsx
autonomous: true
requirements:
  - UI-02

must_haves:
  truths:
    - "TranslationsPage.tsx imports QualityBadge, FilterBar, EntryEditModal, and buildColumns from components/ subfolder"
    - "Each extracted component renders identically to the pre-extraction version — no visual or behavioral changes"
    - "TypeScript compiles with zero errors after extraction"
  artifacts:
    - path: "admin-ui/src/pages/translations/components/types.ts"
      provides: "Shared interfaces: Project, LocaleInfo, ProjectDetails, QualityInfo, Entry, PaginatedEntries, SandboxStatus, DiffEntry, DiffResult, Snapshot, QualityResult, KeyDiff, KeyDiffRow, QualityBadgeProps, EditModalProps, EntriesTableProps"
      contains: "export interface Entry"
    - path: "admin-ui/src/pages/translations/components/QualityBadge.tsx"
      provides: "Quality badge component with all review states"
      contains: "const QualityBadge"
    - path: "admin-ui/src/pages/translations/components/FilterBar.tsx"
      provides: "Filter bar with namespace, search, quality filter, add key button"
      contains: "const FilterBar"
    - path: "admin-ui/src/pages/translations/components/EntryEditModal.tsx"
      provides: "Edit/create modal with AI translate and quality check"
      contains: "const EntryEditModal"
    - path: "admin-ui/src/pages/translations/components/columns.tsx"
      provides: "Table column factory function"
      contains: "export function buildColumns"
    - path: "admin-ui/src/pages/translations/components/api.ts"
      provides: "All API call functions extracted from TranslationsPage"
      contains: "export const fetchEntries"
  key_links:
    - from: "admin-ui/src/pages/translations/TranslationsPage.tsx"
      to: "admin-ui/src/pages/translations/components/types.ts"
      via: "import { Entry, QualityInfo } from './components/types'"
      pattern: "from.*components/types"
    - from: "admin-ui/src/pages/translations/components/columns.tsx"
      to: "admin-ui/src/pages/translations/components/QualityBadge.tsx"
      via: "import QualityBadge in column render"
      pattern: "from.*QualityBadge"
---

<objective>
Extract the monolithic TranslationsPage.tsx (2609 lines) into focused sub-components per D-11, D-12, D-13.

Purpose: Component extraction is the prerequisite for all subsequent visual and filter changes — subsequent plans modify individual extracted files instead of a 2600-line monolith.
Output: 6 new files in `admin-ui/src/pages/translations/components/`, TranslationsPage.tsx reduced to orchestrator role.
</objective>

<execution_context>
@/Users/liubomyrfedyshyn/WebstormProjects/nest_js/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liubomyrfedyshyn/WebstormProjects/nest_js/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-polish/05-CONTEXT.md
@.planning/phases/05-ui-polish/05-RESEARCH.md
@.planning/phases/05-ui-polish/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared types and API module</name>
  <files>
    admin-ui/src/pages/translations/components/types.ts
    admin-ui/src/pages/translations/components/api.ts
  </files>
  <read_first>
    admin-ui/src/pages/translations/TranslationsPage.tsx
    .planning/phases/05-ui-polish/05-RESEARCH.md
  </read_first>
  <action>
Create `admin-ui/src/pages/translations/components/` directory.

**types.ts** — Move ALL interface definitions from TranslationsPage.tsx lines 47-148 into this file. Export each interface. The full list:
- `Project` (id, slug, name)
- `LocaleInfo` (code, isDefault)
- `ProjectDetails` (slug, name, locales, namespaces)
- `QualityInfo` (reviewState union of 7 states, score, level, comment, checkedAt)
- `Entry` (key, createdAt, context, contextNeed, contextReason, values, quality)
- `PaginatedEntries` (data, meta with page/limit/total/totalPages)
- `SandboxStatus` (initialized, initializedAt, hasChanges, snapshotCount)
- `DiffEntry` (namespace, key, locale, status, productionValue, sandboxValue)
- `DiffResult` (total, added, changed, deleted, entries)
- `Snapshot` (id, label, createdAt, entryCount)
- `QualityResult` (score, level, comment)
- `KeyDiff` (namespace, key, status, locales)
- `KeyDiffRow` (id, namespace, key, status, localeEntries)
- `QualityBadgeProps` (info, slug?, namespace?, entryKey?, locale?, isSandbox?, onUpdate?)
- `EditModalProps` (open, entry, locales, isNew, onClose, onSave, saving, projectSlug?, namespace?, isSandbox?, onQualityUpdate?)
- `EntriesTableProps` — the full interface from lines 1131-1158 (projectSlug, queryKeyPrefix, fetchFn, createFn, updateFn, deleteFn, enabled, onMutationSuccess, getRowProps, renderKeyExtra, clientFilter, isSandbox, deleteConfirmTitle, deleteConfirmDescription, extraControls)

**api.ts** — Move ALL API functions from TranslationsPage.tsx lines 149-385 into this file. Import `apiClient` from `../../../api/client`. Import types from `./types`. Export each function as a named export. The full list:
- `fetchProjects`
- `fetchProjectDetails`
- `fetchEntries`
- `createEntry`
- `updateEntry`
- `deleteEntry`
- `aiTranslate`
- `checkQuality`
- `fetchSandboxStatus`
- `fetchSandboxDiff`
- `fetchSnapshots`
- `fetchSandboxEntries`
- `createSandboxEntry`
- `updateSandboxEntry`
- `deleteSandboxEntry`
- `revertSandboxKey`
- `markExpected`
- `unmarkExpected`
- `markSandboxExpected`
- `unmarkSandboxExpected`
- `promoteSelective`

Each function keeps its exact current signature and implementation. The import path for apiClient from api.ts is `../../../api/client` (3 levels up from components/).
  </action>
  <verify>
    <automated>cd /Users/liubomyrfedyshyn/WebstormProjects/nest_js/admin-ui && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - admin-ui/src/pages/translations/components/types.ts contains `export interface Entry`
    - admin-ui/src/pages/translations/components/types.ts contains `export interface QualityInfo`
    - admin-ui/src/pages/translations/components/types.ts contains `export interface QualityBadgeProps`
    - admin-ui/src/pages/translations/components/types.ts contains `export interface EditModalProps`
    - admin-ui/src/pages/translations/components/types.ts contains `export interface EntriesTableProps`
    - admin-ui/src/pages/translations/components/api.ts contains `export const fetchEntries`
    - admin-ui/src/pages/translations/components/api.ts contains `export const aiTranslate`
    - admin-ui/src/pages/translations/components/api.ts contains `export const markExpected`
    - admin-ui/src/pages/translations/components/api.ts contains `export const promoteSelective`
    - admin-ui/src/pages/translations/components/api.ts contains `from '../../../api/client'`
  </acceptance_criteria>
  <done>types.ts exports all 16 interfaces; api.ts exports all 21 API functions; both files compile without errors</done>
</task>

<task type="auto">
  <name>Task 2: Extract QualityBadge, columns, FilterBar, EntryEditModal and rewire TranslationsPage</name>
  <files>
    admin-ui/src/pages/translations/components/QualityBadge.tsx
    admin-ui/src/pages/translations/components/columns.tsx
    admin-ui/src/pages/translations/components/FilterBar.tsx
    admin-ui/src/pages/translations/components/EntryEditModal.tsx
    admin-ui/src/pages/translations/TranslationsPage.tsx
  </files>
  <read_first>
    admin-ui/src/pages/translations/TranslationsPage.tsx
    admin-ui/src/pages/translations/components/types.ts
    admin-ui/src/pages/translations/components/api.ts
    .planning/phases/05-ui-polish/05-RESEARCH.md
    .planning/phases/05-ui-polish/05-UI-SPEC.md
  </read_first>
  <action>
**QualityBadge.tsx** — Extract the QualityBadge component (lines 423-581) and the constants it uses:
- Move `QUALITY_CONFIG` (lines 391-396) and `QUALITY_COLOR` (lines 398-403) into this file as exported constants.
- Move `AI_LOCALES` constant (line 389) here as well (used by EntryEditModal, will import from here).
- Import `QualityBadgeProps`, `QualityInfo` from `./types`.
- Import `markExpected`, `unmarkExpected`, `markSandboxExpected`, `unmarkSandboxExpected` from `./api`.
- Import `Tooltip`, `Popconfirm`, `message` from `antd`.
- Import `SyncOutlined`, `CheckCircleOutlined`, `WarningOutlined` from `@ant-design/icons`.
- Export the component as default: `export default QualityBadge`.
- Keep the exact same rendering logic — no visual changes in this plan.

**columns.tsx** — Extract column definitions into a factory function per RESEARCH.md Pattern 2:
```typescript
import type { ColumnsType } from 'antd/es/table';
import { Entry } from './types';

export function buildColumns(
  locales: string[],
  projectSlug: string,
  namespace: string,
  isSandbox: boolean | undefined,
  onQualityUpdate: () => void,
  onEdit: (entry: Entry) => void,
  onDelete: (key: string) => void,
  renderKeyExtra?: (key: string, namespace: string) => React.ReactNode,
  deleteConfirmTitle?: string,
  deleteConfirmDescription?: string,
): ColumnsType<Entry>
```
- Move the `columns` array from lines 1328-1481 into this function body.
- Import `QualityBadge` from `./QualityBadge`, `QUALITY_COLOR` from `./QualityBadge`.
- Import `Space`, `Tooltip`, `Tag`, `Button`, `Popconfirm`, `Typography` from `antd`.
- Import `EditOutlined`, `DeleteOutlined`, `InfoCircleOutlined`, `WarningOutlined` from `@ant-design/icons`.
- Import `getFlagForCode` from `../../../constants/supported-languages`.
- The function returns the same column structure currently at lines 1328-1481.

**FilterBar.tsx** — Extract the filter bar controls (lines 1485-1555 in the EntriesTable return). Create a FilterBar component:
```typescript
interface FilterBarProps {
  namespace: string;
  namespaces: string[];
  onNamespaceChange: (ns: string) => void;
  searchInput: string;
  onSearchInputChange: (val: string) => void;
  onSearch: () => void;
  qualityLevel: string;
  onQualityLevelChange: (val: string) => void;
  onAddKey: () => void;
  disabled: boolean;
  extraControls?: React.ReactNode;
}
```
- Move the Row/Col layout with Namespace Select, Search Input, Quality Filter Select, and Add Key Button.
- Import `Row`, `Col`, `Select`, `Input`, `Button` from `antd`.
- Import `SearchOutlined`, `PlusOutlined` from `@ant-design/icons`.
- The quality filter options stay as-is for now (will be expanded in Plan 03):
  `[{value:'', label:'All qualities'}, {value:'green', label:'Green'}, {value:'yellow', label:'Yellow'}, {value:'red', label:'Red'}, {value:'expected', label:'Expected'}, {value:'unchecked', label:'Not checked'}, {value:'needs_context', label:'Needs Context'}]`
- Export FilterBarProps interface from types.ts as well.
- Export default FilterBar.

**EntryEditModal.tsx** — Extract the EditModal component (lines 603-1128):
- Import `EditModalProps`, `QualityResult`, `QualityInfo` from `./types`.
- Import `aiTranslate`, `checkQuality`, `markExpected`, `unmarkExpected`, `markSandboxExpected`, `unmarkSandboxExpected` from `./api`.
- Import `AI_LOCALES`, `QUALITY_COLOR`, `QUALITY_CONFIG` from `./QualityBadge`.
- Import all antd components used by the modal: `Modal`, `Form`, `Input`, `Button`, `Space`, `Tooltip`, `Tag`, `Popconfirm`, `Checkbox`, `message`.
- Import icons: `ThunderboltOutlined`, `SafetyCertificateOutlined`, `CheckCircleOutlined`, `WarningOutlined`.
- Rename from `EditModal` to `EntryEditModal` for clarity. Export as default.
- Keep exact same logic — no changes to AI translate, quality check, or mark-expected handlers.

**TranslationsPage.tsx** — Rewire the orchestrator:
- Remove all interface definitions (now in types.ts) — replace with `import { ... } from './components/types'`.
- Remove all API functions (now in api.ts) — replace with `import { ... } from './components/api'`.
- Remove QualityBadge component, QUALITY_CONFIG, QUALITY_COLOR, AI_LOCALES, ROW_BG constants — import from components.
  - Keep `ROW_BG` in TranslationsPage.tsx (only used by SandboxTab, not extracted).
- Remove EditModal component — replace with `import EntryEditModal from './components/EntryEditModal'`. Update JSX from `<EditModal` to `<EntryEditModal`.
- Remove inline columns array from EntriesTable — replace with `import { buildColumns } from './components/columns'`. Wrap in useMemo:
  ```typescript
  const columns = useMemo(
    () => buildColumns(locales, projectSlug, namespace, isSandbox, invalidate, 
      (entry) => { setEditEntry(entry); setIsNewEntry(false); setEditModalOpen(true); },
      (key) => deleteMutation.mutate(key),
      renderKeyExtra, deleteConfirmTitle, deleteConfirmDescription),
    [locales, projectSlug, namespace, isSandbox, invalidate, renderKeyExtra, deleteConfirmTitle, deleteConfirmDescription]
  );
  ```
- Remove filter bar JSX from EntriesTable return — replace with `import FilterBar from './components/FilterBar'`.
  Wire FilterBar props from existing EntriesTable state.
- The SandboxTab, SandboxDiffModal, and main TranslationsPage component stay in TranslationsPage.tsx — they are large and not part of D-11.
  </action>
  <verify>
    <automated>cd /Users/liubomyrfedyshyn/WebstormProjects/nest_js/admin-ui && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - admin-ui/src/pages/translations/components/QualityBadge.tsx contains `const QualityBadge`
    - admin-ui/src/pages/translations/components/QualityBadge.tsx contains `export default QualityBadge`
    - admin-ui/src/pages/translations/components/QualityBadge.tsx contains `QUALITY_COLOR`
    - admin-ui/src/pages/translations/components/columns.tsx contains `export function buildColumns`
    - admin-ui/src/pages/translations/components/columns.tsx contains `from './QualityBadge'`
    - admin-ui/src/pages/translations/components/FilterBar.tsx contains `const FilterBar`
    - admin-ui/src/pages/translations/components/EntryEditModal.tsx contains `const EntryEditModal`
    - admin-ui/src/pages/translations/components/EntryEditModal.tsx contains `from './api'`
    - admin-ui/src/pages/translations/TranslationsPage.tsx contains `from './components/types'`
    - admin-ui/src/pages/translations/TranslationsPage.tsx contains `from './components/api'`
    - admin-ui/src/pages/translations/TranslationsPage.tsx contains `from './components/columns'`
    - admin-ui/src/pages/translations/TranslationsPage.tsx contains `useMemo`
    - TypeScript compiles with zero errors: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>QualityBadge, columns, FilterBar, and EntryEditModal extracted to components/ subfolder; TranslationsPage imports and renders them; app compiles and renders identically to pre-extraction state</done>
</task>

</tasks>

<verification>
1. `cd admin-ui && npx tsc --noEmit` — zero TypeScript errors
2. `cd admin-ui && npm run build` — Vite build succeeds
3. Visual spot-check: `cd admin-ui && npm run dev` — translations page loads, table renders, filter bar works, edit modal opens, quality badges display
</verification>

<success_criteria>
- TranslationsPage.tsx reduced from ~2609 lines to ~1400-1600 lines (SandboxTab + orchestration remain)
- 6 new files created in components/ subfolder
- Zero TypeScript compilation errors
- No visual or behavioral changes — pure structural refactor
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-polish/05-01-SUMMARY.md`
</output>
